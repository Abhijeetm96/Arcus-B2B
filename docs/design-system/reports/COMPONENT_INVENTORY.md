# 🗃️ ARCUS Component Inventory

Detailed counts of HTML components found in the frontend source code:

---

## 1. Raw UI Components Inventory

* **Button Elements**: **330 instances** (needs standardisation using `Button` component).
* **Input Elements**: **248 instances** (needs standardisation using `Input` component).
* **Select Dropdowns**: **51 instances** (needs standardisation using `Select` component).
* **Tables**: **26 tables** (needs standardisation using standard table headers/cells).
* **Dialogs / Modals**: **111 instances** (needs standardisation using drawers/sheets).
