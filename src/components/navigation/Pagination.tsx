/**
 * ARCUS Compatibility Adapter
 *
 * Re-exports the standard components from the UI directory
 * to prevent breaking legacy imports of Pagination.
 */

export {
  Pagination,
  type PaginationProps,
} from '../ui/Pagination';
